# Flicker Picture Search By Geolocation

## How to run 
- create a .env file in the root of the project where .dockerfile resides
- inside that env file put your flickr api key in the following way
  - *flickr_api_key*="your api key"
- once that is done, simply run the following command
```
docker-compose up --build
```
